# 🚀 实时运行指南

## 方法1：交互模式（最推荐）

在你的新终端运行：

```bash
cd /Users/renqing/novel-game-framework
./RUN_INTERACTIVE.sh
```

**优点**：
- ✅ 实时看到所有输出
- ✅ 看到 Claude 的思考和操作
- ✅ 完成后自动询问是否继续
- ✅ 简单直接

## 方法2：使用 expect 强制实时输出

如果上面的方法仍然卡住，安装 expect：

```bash
# macOS
brew install expect

# 然后运行
./RUN_INTERACTIVE.sh
```

## 方法3：监控 Claude 日志

在**另一个终端窗口**运行：

```bash
# 实时查看 Claude 的内部日志
tail -f ~/.local/share/claude/log/$(ls -t ~/.local/share/claude/log/ | head -1)
```

同时在主终端运行：
```bash
cd /Users/renqing/novel-game-framework
./RUN_INTERACTIVE.sh
```

## 方法4：最简单的方式

直接在你的终端运行：

```bash
cd /Users/renqing/novel-game-framework

# 生成任务
python3 smart_task_generator.py > CURRENT_TASK.md

# 运行 Claude（实时输出）
echo "请执行 CURRENT_TASK.md 的所有任务，自动完成并提交。" | claude --permission-mode bypassPermissions
```

## 🔍 检查是否在运行

```bash
# 查看 Claude 进程
ps aux | grep claude | grep -v grep

# 查看最新进度
cat tools/progress.json

# 查看 Git 日志
git log --oneline -3
```

## ⚠️ 如果还是卡住

1. 按 Ctrl+C 停止
2. 检查是否有多个 Claude 进程：
   ```bash
   killall claude
   ```
3. 重新运行

## 💡 推荐的工作流

**在 tmux 中运行**（推荐）：
```bash
# 创建新会话
tmux new -s novel

# 运行自动化
cd /Users/renqing/novel-game-framework
./RUN_INTERACTIVE.sh

# 分离会话：按 Ctrl+B 然后 D
# 重新连接：tmux attach -t novel
```
