# 🤖 自动小说处理系统使用指南

## 📋 目录

1. [系统介绍](#系统介绍)
2. [快速开始](#快速开始)
3. [功能特性](#功能特性)
4. [详细使用说明](#详细使用说明)
5. [配置选项](#配置选项)
6. [常见问题](#常见问题)
7. [高级用法](#高级用法)

---

## 🎯 系统介绍

自动小说处理系统是一个**完全自动化**的工具，能够：

✅ **逐章阅读和分析小说**（不跳过任何步骤）
✅ **每10章自动回顾检查**质量和进度
✅ **自动批准并继续**下一批次处理
✅ **完整处理整个小说**直到生成游戏数据
✅ **支持中断恢复**，随时可以继续

### 工作流程

```
开始
  ↓
读取第N章 → AI分析 → 提取角色/地点/物品 → 更新世界观
  ↓
第N+1章 → AI分析 → 提取信息 → 更新
  ↓
...
  ↓
第10章完成 → 自动回顾检查 → 自动批准 → 继续
  ↓
...
  ↓
全部完成 → 生成最终游戏数据
```

---

## 🚀 快速开始

### 方法一：一键启动（推荐）

**最简单的方式！**只需要一条命令：

```bash
# 1. 设置API Key（只需一次）
export DEEPSEEK_API_KEY='your-api-key-here'

# 2. 给脚本执行权限
chmod +x start_auto_writer.sh

# 3. 一键启动！
./start_auto_writer.sh
```

就这么简单！脚本会自动处理整个小说。

### 方法二：Python命令行

如果您想要更多控制：

```bash
python3 auto_novel_writer.py \
    --novel novel/斗破苍穹.txt \
    --name "斗破苍穹游戏" \
    --api_key YOUR_API_KEY \
    --auto-approve
```

---

## ✨ 功能特性

### 1. 完全自动化

- ✅ 自动读取每一章（**不跳过阅读**）
- ✅ 自动调用9个AI分析器
- ✅ 自动提取角色、地点、物品、设定
- ✅ 自动更新世界观和角色关系
- ✅ 自动生成章节摘要

### 2. 智能回顾系统

每10章（可配置）自动执行：

```
🔍 回顾检查
├── 📊 统计累积发现（角色、地点、物品数量）
├── 📝 显示最近章节摘要
├── ✅ 自动批准或等待人工确认
└── 🚀 继续下一批次
```

### 3. 进度保护

- 💾 **实时保存进度**：每章处理完立即保存
- 🔄 **支持中断恢复**：Ctrl+C 或错误后可继续
- 📊 **详细状态显示**：随时了解处理进度
- 📝 **完整日志记录**：所有操作都有日志

### 4. 质量保证

- 🔍 **不跳过任何步骤**：确保每章都被完整分析
- 📖 **完整阅读内容**：不省略任何文本
- 🎯 **精确提取信息**：使用9个专业AI分析器
- ✅ **定期回顾检查**：确保质量稳定

---

## 📖 详细使用说明

### 步骤 1: 准备环境

#### 1.1 安装依赖

```bash
# 进入项目目录
cd novel-game-framework

# 安装Python依赖
pip install -r backend/requirements.txt

# 初始化Django数据库
cd backend
python manage.py migrate
cd ..
```

#### 1.2 准备API Key

获取DeepSeek API Key（或OpenAI API Key）：

```bash
# 设置环境变量（推荐）
export DEEPSEEK_API_KEY='sk-your-key-here'

# 或者在命令中直接提供
python3 auto_novel_writer.py --api_key 'sk-your-key-here' ...
```

#### 1.3 准备小说文件

确保您的小说文件在 `novel/` 目录下：

```bash
ls novel/
# 应该看到: 斗破苍穹.txt
```

### 步骤 2: 启动自动处理

#### 方式A：完全自动模式（推荐用于长时间运行）

```bash
./start_auto_writer.sh
```

或者：

```bash
python3 auto_novel_writer.py \
    --novel novel/斗破苍穹.txt \
    --name "斗破苍穹游戏" \
    --api_key $DEEPSEEK_API_KEY \
    --auto-approve \
    --review-interval 10
```

**特点**：
- ✅ 完全自动，无需人工干预
- ✅ 每10章自动回顾并批准
- ✅ 适合长时间运行（可以去睡觉）

#### 方式B：交互模式（适合测试或小范围处理）

```bash
python3 auto_novel_writer.py \
    --novel novel/斗破苍穹.txt \
    --name "斗破苍穹游戏" \
    --api_key $DEEPSEEK_API_KEY \
    --review-interval 10
```

**特点**：
- 🔍 每10章暂停，等待人工确认
- 📊 可以查看详细统计信息
- ✅ 适合测试和质量把控

### 步骤 3: 监控进度

系统会实时显示处理状态：

```
======================================================================
📊 当前状态
======================================================================
当前章节: 23/1694 (1.36%)
已处理章节: 1
已完成回顾: 0
运行时间: 0:05:32.123456
======================================================================

📖 正在处理第 24 章...
   字数: 4521
   ✅ 分析完成 (耗时: 8.45秒)
   📝 核心事件: 萧炎在魔兽山脉遇到了云韵...
```

### 步骤 4: 查看结果

处理完成后，数据会保存到Django数据库中：

```bash
# 启动Web界面查看
cd backend
python manage.py runserver

# 访问 http://localhost:8000
```

---

## ⚙️ 配置选项

### 基本参数

| 参数 | 说明 | 必需 | 默认值 | 示例 |
|-----|------|------|-------|------|
| `--novel` | 小说文件路径 | ✅ | 无 | `novel/斗破苍穹.txt` |
| `--name` | 项目名称 | ✅ | 无 | `"斗破苍穹游戏"` |
| `--api_key` | API密钥 | ✅* | `$DEEPSEEK_API_KEY` | `sk-xxx` |
| `--provider` | AI提供商 | ❌ | `deepseek` | `deepseek` / `openai` |

*注：如果设置了环境变量 `DEEPSEEK_API_KEY`，则可省略

### 高级参数

| 参数 | 说明 | 默认值 | 推荐值 |
|-----|------|-------|-------|
| `--review-interval` | 回顾间隔（章节数） | 10 | 10（短篇） / 20（长篇） |
| `--auto-approve` | 自动批准回顾 | False | True（长时间运行） |
| `--resume` | 从上次中断继续 | False | 根据需要 |

### 配置示例

#### 示例1：快速测试（小说前50章）

```bash
python3 auto_novel_writer.py \
    --novel novel/test_novel.txt \
    --name "测试项目" \
    --api_key $DEEPSEEK_API_KEY \
    --review-interval 5
```

#### 示例2：长篇小说完全自动

```bash
python3 auto_novel_writer.py \
    --novel novel/斗破苍穹.txt \
    --name "斗破苍穹游戏" \
    --api_key $DEEPSEEK_API_KEY \
    --auto-approve \
    --review-interval 20
```

#### 示例3：从中断处继续

```bash
python3 auto_novel_writer.py \
    --novel novel/斗破苍穹.txt \
    --name "斗破苍穹游戏" \
    --api_key $DEEPSEEK_API_KEY \
    --auto-approve \
    --resume
```

---

## ❓ 常见问题

### Q1: 如何中断处理？

**A**: 按 `Ctrl+C` 即可安全中断。进度会自动保存，下次使用 `--resume` 继续。

```bash
# 中断后继续
python3 auto_novel_writer.py --resume --api_key $DEEPSEEK_API_KEY
```

### Q2: 处理速度如何？

**A**: 取决于API速度和章节长度：

- **短章节**（3000字）：约 5-10 秒/章
- **长章节**（10000字）：约 15-30 秒/章
- **1694章小说**：预计 8-15 小时（完全自动）

### Q3: 如何查看详细进度？

**A**: 有三种方式：

1. **实时输出**：脚本会实时显示处理状态
2. **查看日志**：`logs/auto_writer_YYYYMMDD_HHMMSS.log`
3. **查看进度文件**：`tools/progress.json`

```bash
# 查看当前进度
cat tools/progress.json

# 实时监控日志
tail -f logs/auto_writer_*.log
```

### Q4: 出错了怎么办？

**A**: 系统有完善的错误处理：

1. **单章错误**：记录错误并继续下一章
2. **连续错误**：在交互模式下会询问是否继续
3. **致命错误**：保存进度并退出，可以 `--resume` 继续

查看错误详情：

```bash
# 查看最新日志
cat logs/auto_writer_*.log | grep "❌"

# 查看进度状态
python3 -c "import json; print(json.dumps(json.load(open('tools/progress.json')), indent=2))"
```

### Q5: 如何重新开始？

**A**: 删除进度文件和数据库：

```bash
# 删除进度
rm tools/progress.json

# 删除数据库（可选）
rm backend/db.sqlite3

# 重新初始化
cd backend
python manage.py migrate
cd ..

# 重新开始
./start_auto_writer.sh
```

### Q6: API额度不够了怎么办？

**A**: 系统会保存进度，更换API Key后继续：

```bash
# 更换API Key
export DEEPSEEK_API_KEY='new-api-key'

# 继续处理
python3 auto_novel_writer.py --resume --api_key $DEEPSEEK_API_KEY
```

---

## 🔧 高级用法

### 1. 在后台运行

适合长时间处理，即使关闭终端也能继续：

```bash
# 使用 nohup 在后台运行
nohup ./start_auto_writer.sh > auto_writer.out 2>&1 &

# 查看进程
ps aux | grep auto_novel_writer

# 查看实时输出
tail -f auto_writer.out

# 停止进程
kill <PID>
```

### 2. 使用 Screen 或 Tmux

更灵活的后台运行方式：

```bash
# 创建新的 screen 会话
screen -S novel_writer

# 在 screen 中运行
./start_auto_writer.sh

# 分离会话: Ctrl+A, 然后按 D

# 重新连接
screen -r novel_writer
```

### 3. 定时自动处理

使用 cron 在特定时间自动处理：

```bash
# 编辑 crontab
crontab -e

# 添加定时任务（每天凌晨2点运行）
0 2 * * * cd /path/to/novel-game-framework && ./start_auto_writer.sh >> logs/cron.log 2>&1
```

### 4. 批量处理多本小说

创建批处理脚本：

```bash
#!/bin/bash
# batch_process.sh

novels=(
    "novel/斗破苍穹.txt|斗破苍穹游戏"
    "novel/斗罗大陆.txt|斗罗大陆游戏"
    "novel/完美世界.txt|完美世界游戏"
)

for item in "${novels[@]}"; do
    IFS='|' read -r novel_path project_name <<< "$item"

    echo "开始处理: $project_name"

    python3 auto_novel_writer.py \
        --novel "$novel_path" \
        --name "$project_name" \
        --api_key $DEEPSEEK_API_KEY \
        --auto-approve

    echo "完成: $project_name"
    echo "---"
done
```

### 5. 自定义回顾逻辑

修改 `auto_novel_writer.py` 中的 `_perform_review()` 方法，添加自定义检查：

```python
def _perform_review(self, chapter_num: int):
    # 原有回顾逻辑...

    # 添加自定义检查
    if self.stats['chapters_processed'] > 100:
        # 检查角色数量是否合理
        if len(self.processor.accumulated_context['已识别角色']) < 5:
            print("⚠️  警告：角色数量偏少，可能存在问题")
            return False

    # 继续原有逻辑...
```

---

## 📊 监控和调试

### 查看实时状态

```bash
# 方式1：查看进度文件
watch -n 1 cat tools/progress.json

# 方式2：查看日志最后20行
watch -n 1 'tail -20 logs/auto_writer_*.log'

# 方式3：使用 Python 脚本
python3 tools/progress_tracker.py --status
```

### 性能监控

```bash
# 监控CPU和内存使用
top -p $(pgrep -f auto_novel_writer)

# 监控磁盘IO
iotop -p $(pgrep -f auto_novel_writer)

# 查看数据库大小
du -h backend/db.sqlite3
```

### 调试模式

添加详细日志输出：

```python
# 在 auto_novel_writer.py 开头添加
import logging
logging.basicConfig(level=logging.DEBUG)
```

---

## 📝 日志说明

### 日志级别

- ✅ **成功**：绿色，表示操作成功
- 📊 **信息**：蓝色，表示状态信息
- ⚠️ **警告**：黄色，表示需要注意的情况
- ❌ **错误**：红色，表示发生错误

### 日志文件位置

```
logs/
├── auto_writer_20260113_141520.log    # 自动命名，包含时间戳
├── auto_writer_20260113_183045.log
└── cron.log                           # 如果使用 cron
```

### 日志分析

```bash
# 统计成功章节数
grep "✅ 分析完成" logs/auto_writer_*.log | wc -l

# 查看所有错误
grep "❌" logs/auto_writer_*.log

# 查看回顾记录
grep "🔍" logs/auto_writer_*.log

# 查看处理速度
grep "耗时" logs/auto_writer_*.log | awk '{print $NF}' | sed 's/秒)//' | awk '{sum+=$1; n++} END {print sum/n " 秒/章"}'
```

---

## 🎉 完成后的使用

处理完成后，您可以：

### 1. 启动Web界面

```bash
cd backend
python manage.py runserver

# 访问 http://localhost:8000
```

### 2. 导出游戏数据

```python
# export_game_data.py
import json
from backend.game.models import GameProject

project = GameProject.objects.get(name="斗破苍穹游戏")

data = {
    'world_setting': project.world_setting,
    'characters': project.characters,
    'story_tree': project.story_tree,
    'attributes': project.attributes,
    'items': project.items,
    'exploration': project.exploration,
}

with open('game_data.json', 'w', encoding='utf-8') as f:
    json.dump(data, f, ensure_ascii=False, indent=2)

print("✅ 游戏数据已导出到 game_data.json")
```

### 3. 部署游戏

将生成的数据部署到游戏前端：

```bash
# 复制到前端数据目录
cp game_data.json frontend/data/

# 启动前端
cd frontend
python -m http.server 8080

# 访问 http://localhost:8080
```

---

## 📞 技术支持

如果遇到问题：

1. **查看本文档**的常见问题部分
2. **检查日志文件**了解详细错误信息
3. **查看代码注释**了解实现细节
4. **提交 Issue** 到项目仓库

---

## 📄 许可证

本项目遵循 MIT 许可证。

---

**祝您使用愉快！🎉**

如有任何问题，欢迎随时提问。
