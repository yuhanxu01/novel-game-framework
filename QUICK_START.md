# 🚀 快速启动指南

## 一键启动自动小说处理系统

### 第一步：设置 API Key

```bash
export DEEPSEEK_API_KEY='your-api-key-here'
```

### 第二步：启动系统

```bash
./start_auto_writer.sh
```

就这么简单！🎉

---

## 系统会自动完成以下工作：

✅ **逐章阅读小说**（不跳过任何步骤）
✅ **AI分析每一章**（提取角色、地点、物品、设定）
✅ **每10章自动回顾**（检查质量和进度）
✅ **自动批准并继续**（无需人工干预）
✅ **处理完整部小说**（直到生成游戏数据）

---

## 当前进度

根据 `tools/progress.json`，系统已处理到：

- **当前章节**: 22/1694
- **完成进度**: 1.3%
- **状态**: 可以继续处理

---

## 监控进度

### 方式1：实时查看日志

```bash
tail -f logs/auto_writer_*.log
```

### 方式2：查看进度文件

```bash
cat tools/progress.json
```

### 方式3：使用进度跟踪器

```bash
python3 tools/progress_tracker.py --status
```

---

## 中断和恢复

### 中断处理

按 `Ctrl+C` 即可安全中断，进度会自动保存。

### 恢复处理

再次运行启动脚本，系统会自动检测并继续：

```bash
./start_auto_writer.sh
```

---

## 手动控制模式

如果您想在每10章手动确认，而不是自动批准：

### 方式1：编辑启动脚本

打开 `start_auto_writer.sh`，修改：

```bash
AUTO_APPROVE=false  # 改为 false
```

### 方式2：直接使用 Python 命令

```bash
python3 auto_novel_writer.py \
    --novel novel/斗破苍穹.txt \
    --name "斗破苍穹游戏" \
    --api_key $DEEPSEEK_API_KEY \
    --review-interval 10
    # 注意：不加 --auto-approve 参数
```

---

## 配置选项

所有配置都在 `start_auto_writer.sh` 中：

```bash
NOVEL_PATH="novel/斗破苍穹.txt"       # 小说文件路径
PROJECT_NAME="斗破苍穹游戏"            # 项目名称
API_PROVIDER="deepseek"               # AI提供商
REVIEW_INTERVAL=10                    # 回顾间隔（章节数）
AUTO_APPROVE=true                     # 自动批准
```

根据需要修改这些值即可。

---

## 预计处理时间

根据章节长度和API速度：

- **单章处理**: 5-30 秒
- **10章回顾**: 1-2 分钟
- **完整小说** (1694章): 约 **8-15 小时**

建议：
- 💡 使用**完全自动模式** (`AUTO_APPROVE=true`)
- 💡 在**后台运行** (`nohup ./start_auto_writer.sh &`)
- 💡 选择**夜间处理**（可以去睡觉）

---

## 后台运行

### 使用 nohup

```bash
nohup ./start_auto_writer.sh > auto_writer.out 2>&1 &

# 查看输出
tail -f auto_writer.out

# 查看进程
ps aux | grep auto_novel_writer
```

### 使用 screen

```bash
# 创建新会话
screen -S novel_writer

# 运行脚本
./start_auto_writer.sh

# 分离会话: Ctrl+A, 然后按 D

# 重新连接
screen -r novel_writer
```

---

## 完成后

处理完成后，游戏数据会保存在 Django 数据库中。

### 查看结果

```bash
cd backend
python manage.py runserver

# 访问 http://localhost:8000
```

### 导出数据

数据已自动保存到数据库，可以通过 Web 界面或 API 访问。

---

## 需要帮助？

📖 **详细文档**: 查看 `AUTO_WRITER_README.md`
❓ **常见问题**: 在详细文档中有完整的 FAQ
🐛 **遇到错误**: 查看 `logs/` 目录下的日志文件

---

**现在就开始吧！** 🎉

```bash
export DEEPSEEK_API_KEY='your-key'
./start_auto_writer.sh
```
